
services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: customer-api
    # Use 'expose' instead of 'ports' to keep it internal only
    expose:
      - "8000"
    volumes:
      # Mount models folder for future model loading
      - ./models:/app/models:ro
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - customer-network
    restart: unless-stopped

  streamlit:
    build:
      context: ./streamlit
      dockerfile: Dockerfile
    container_name: customer-streamlit
    # Only the Streamlit app is publicly accessible
    ports:
      - "8501:8501"
    volumes:
      # Mount data folder for future JSON schema loading
      - ./data:/app/data:ro
      - ./streamlit/app.py:/app/app.py
    environment:
      - API_URL=http://api:8000
      - PYTHONUNBUFFERED=1
    networks:
      - customer-network
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

networks:
  customer-network:
    driver: bridge